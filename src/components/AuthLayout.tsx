// AuthLayout - simplified to avoid type errors
// Not used in our app, but kept to satisfy imports
export function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
